# Global config
global:
  scrape_interval:     5s 
  evaluation_interval: 5s

# Scrape configurations
scrape_configs:
  # From Prometheus itself
  - job_name: 'ShortTermDB'
    metrics_path: /metrics
    scrape_interval: 5s
    static_configs:
    - targets: ["{$server_name}:{$server_ip}"]

  #From node-exporter
  - job_name: 'WorkstationMetrics'
    scrape_interval: 5s
    static_configs:
    - targets: ["{$server_name}:{$server_ip}",{$all_clients}]
